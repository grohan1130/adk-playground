name: adk-playground
description: Weather and time agent with Google ADK

# Filesystem persistence configuration
storage:
  type: filesystem
  path: ./persisted_storage

# Agent, tool, and workflow paths
paths:
  #agents: ./agents
  #tools: ./tools
  #workflows: ./workflows
  storage: ./persisted_storage

# Runtime configuration
runtime:
  host: 0.0.0.0
  port: 8080
